# Hello_agent 示例科目

空书桌第一次启动时，知库会把本目录的 `Hello-Agents.md` 收成科目 **Hello_agent**。

正文摘自 Datawhale《Hello-Agents》（从零开始构建智能体）。原书 PDF 约 100MB，仓库只带提取后的 Markdown，避免把本机 `data/`（含 API Key、对话、向量）推上网。

克隆后双击 `知库.app` 或跑 `./run.sh`，书桌里应直接看到这个科目。已经有科目的本机库不会被覆盖。
